<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AWArtis"
             x:Class="AWArtis.MainPage">
   
    <StackLayout>
    <StackLayout>
        <Entry x:Name="entryCodigo" Completed="OnCodigoCompleted" />
        <Entry x:Name="entryDescripcion" Completed="OnDescripcionCompleted" />
        </StackLayout>
    <StackLayout>
        <Button Text="Buscar" x:Name="BtnBuscar" 
           HorizontalOptions="CenterAndExpand"
           VerticalOptions="CenterAndExpand" Clicked="btnBuscar_Clicked"></Button>
        <Button Text="Leer código" x:Name="BtnLeerCodigo"
           HorizontalOptions="CenterAndExpand"
           VerticalOptions="CenterAndExpand" Clicked="btnLeerCodigo_Clicked" ></Button>
            <Button Text="Leer2 código" x:Name="BtnPrueba"
           HorizontalOptions="CenterAndExpand"
           VerticalOptions="CenterAndExpand" Clicked="btnPrueba_Clicked" ></Button>
        </StackLayout>
    </StackLayout>
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="edit" Order="Primary" Clicked="OnToolbarItemClicked">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource" Android="baseline_more_vert_white_24.png"/>
            </ToolbarItem.Icon>
        </ToolbarItem>
        <ToolbarItem Text="Otro" Order="Secondary" Clicked="OnToolbarItemClicked">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource" Android="baseline_more_vert_white_18.png"/>
            </ToolbarItem.Icon>
        </ToolbarItem>
        <ToolbarItem Text="Configuración" Order="Secondary" Clicked="OnToolbarItemClicked">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource" Android="baseline_more_vert_white_18.png"/>
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>
</ContentPage>
